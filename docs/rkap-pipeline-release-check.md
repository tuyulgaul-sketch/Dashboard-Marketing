# RKAP Pipeline matrix release check

The blank XLSX template uses an explicit numeric TOTAL PREMI. The importer independently sums all twelve months and rejects mismatches. Formula cells without cached results remain rejected. The guarded integration workflow must pass before merging.

This file contains no production data or credentials.
